{
  "name": "shn-kit-desktop",
  "private": true,
  "version": "0.9.2",
  "main": "dist/main.js",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -b",
    "dev": "npm run build && electron dist/main.js",
    "pack": "npm run build && electron-builder --dir"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "electron": "37.10.3",
    "electron-builder": "26.15.3",
    "typescript": "~6.0.2",
    "vitest": "^4.1.8"
  }
}
